# Template — copy values from Supabase dashboard; do not commit real secrets to public repos.
# Enable Row Level Security on all tables exposed to anon key.
SUPABASE_URL=
SUPABASE_ANON_KEY=
POWERSYNC_URL=

# Google AI Studio / Gemini API — optional; offline keyword triage runs without it.
GEMINI_API_KEY=
