1. **Fix Linter Warnings**
   - In `lib/services/webrtc_voice_call_service.dart`, remove the `_ref` parameter from the class but keep `Ref ref` in the constructor to fix the `unused_field` lint.
   - In `lib/services/family_circle_service.dart`, remove the `_ref` parameter from the class but keep `Ref ref` in the constructor to fix the `unused_field` lint.
   - In `l10n.yaml`, add `header: ""` to prevent the `unused_import` generation.
   - In `analysis_options.yaml`, ignore `unused_import` for `lib/l10n/**` to avoid linting on autogenerated files.
   - Run `flutter gen-l10n` to regenerate localization files without the `unused_import` comment.
   - Run `find lib/l10n -type f -name "*.dart" -exec sed -i '/\/\/ ignore: unused_import/d' {} +` to clean any existing ignored imports.
   - In `lib/ui/first_aid_screen.dart`, import `unawaited` from `dart:async` and replace `// ignore: discarded_futures` with `unawaited(...)`.

2. **Run tests & verify**
   - `flutter pub get`
   - `dart analyze`
   - `flutter test`

3. **Pre-commit Instructions**
   - Complete pre commit steps to make sure proper testing, verifications, reviews and reflections are done.

4. **Submit PR**
   - Branch: `fix-lint-warnings`
   - Commit: `🧹 [Lint] Clean up unused fields, discard futures and l10n imports`
